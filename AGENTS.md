# AGENTS.md

## TDD 开发要求

本项目采用测试驱动开发（TDD）。实现新功能、修复缺陷或改变既有行为时，必须遵循 **Red → Green → Refactor**：

1. **Red（先写失败测试）**
   - 先明确可观察的行为和边界条件，再补充测试。
   - 测试应在实现变更前失败，并能准确说明缺失或错误的行为。
   - 缺陷修复必须先添加能够复现该缺陷的回归测试。
2. **Green（最小实现）**
   - 只编写使测试通过所需的最小实现。
   - 不要通过删除、放宽断言或跳过测试来获得“通过”。
3. **Refactor（保持测试通过的重构）**
   - 在测试持续通过的前提下改善结构、可读性和复用性。
   - 重构完成后重新运行完整测试套件。

## 测试约定

- 优先测试公共行为，而不是实现细节。
- 单元测试放在相关 Rust 模块中；跨模块或文件系统行为使用集成测试。
- 测试名称应描述场景和预期结果，例如 `renaming_session_updates_index`。
- 测试应隔离外部状态；涉及文件系统时使用临时目录，不要修改真实的 `~/.claude/` 数据。
- 对错误路径、空输入、边界值和异步/并发行为补充测试。
- 修改现有行为时，先确认现有测试是否覆盖该行为；不足时先补测试。

## 提交前验证

在完成实现或重构后运行：

```bash
cargo fmt --check
cargo test
cargo clippy --all-targets --all-features -- -D warnings
```

如果某项命令无法运行，必须在交付说明中明确原因；不得隐瞒未运行的验证。提交代码时应说明新增或修改了哪些测试，以及测试覆盖的行为。
